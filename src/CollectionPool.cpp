#include "CollectionPool.hpp"

template<typename T, bool is_const>
T & PoolIterator<T, is_const>::operator*()
{
	// TODO: insérer une instruction return ici
}

template<typename T, bool is_const>
const T & PoolIterator<T, is_const>::operator*() const
{
	// TODO: insérer une instruction return ici
}